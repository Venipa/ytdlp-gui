/**
 * @type {import('eslint').Linter.Config}
 * @type {import('@electron-toolkit/eslint-config-ts/eslint-recommended')}
 */
const config = {
  extends: [],
  rules: {
    'unused-vars': 'off',
  }
}

module.exports = config;
